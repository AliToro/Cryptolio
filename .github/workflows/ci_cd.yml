name: scp files

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: test
      run: |
        cd /home/cryptolio-py-api/codes/